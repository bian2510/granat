<div class="search-container container-fluid">
  <div class="row justify-content-center align-self-start">
    <div class="col-12 col-md-3">
      <h2>Encuentra tu nuevo hogar</h2>
      <form class="d-flex">
        <input class="form-control me-2" type="search" placeholder="Provincia, Cantón" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </div>
</div>

<!-- -->
<%= render 'about_us'%>

<!-- Neighborhoods -->
<%= render 'features_neighborhoods'%>

<!-- Properties -->
<%= render 'features_properties'%>