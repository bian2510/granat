<div class="search-container container-fluid">
  <div class="row justify-content-around align-self-start h-100 blur">
    <div class="col-12 col-md-3 align-self-center">
      <h2 class='text-light'>Busque su barrio para vivir</h2>
      <form class="d-flex">
        <select class="form-select bg-dark text-white w-50 custom-select" aria-label="Default select example">
          <option selected>Operación</option>
          <option value="1">Alquiler</option>
          <option value="2">Compra</option>
        </select>
        <input class="form-control me-2" type="search" placeholder="Caballito, Almagro" aria-label="Search">
        <button class="btn btn-dark" type="submit">Buscar</button>
      </form>
    </div>
  </div>
</div>

<!-- about us -->
<%# <%= render 'main/partials/about_us'%>

<!-- Neighborhoods -->
<%= render 'main/partials/features_neighborhoods'%>

<!-- Properties -->
<%= render 'main/partials/features_properties'%>