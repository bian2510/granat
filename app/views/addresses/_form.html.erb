<%= form_with(model: address) do |form| %>
  <% if address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(address.errors.count, "error") %> prohibited this address from being saved:</h2>

      <ul>
        <% address.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :province %>
    <%= form.text_field :province %>
  </div>

  <div class="field">
    <%= form.label :city %>
    <%= form.text_field :city %>
  </div>

  <div class="field">
    <%= form.label :municipality %>
    <%= form.select :municipality, options_for_select(neighboords[:caba]), {}, class: "form-select" %>
  </div>

  <div class="field">
    <%= form.label :neighborhood %>
    <%= form.select :neighborhood, options_for_select(neighboords[:caba]), {}, class: "form-select" %>
  </div>

  <div class="field">
    <%= form.label :main_street %>
    <%= form.text_field :main_street %>
  </div>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number %>
  </div>

  <div class="field">
    <%= form.label :secondary_street %>
    <%= form.text_field :secondary_street %>
  </div>

  <div class="field">
    <%= form.label :property_id %>
    <%= form.text_field :property_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
